/*
	This file was generated by XVT-Design 4.5, a product of:

		XVT Software Inc.
		4900 Pearl East Circle
		Boulder, CO USA 80301
		303-443-4223, fax 303-443-0969

	Generated on Tue Sep 02 10:34:59 1997
*/
/* TAG BEGIN SPCL:Pre_Header */
/* TAG END SPCL:Pre_Header */

#include "xvt.h"
#include "xvtcm.h"
#include "nodInc.h"

/* 
	Information about the window
*/
#define WIN_RES_ID GEOLOGY_OPTIONS_WINDOW
#define WIN_FLAGS 0x810L
#define WIN_CLASS ""
#define WIN_BORDER W_DOC
/* TAG BEGIN SPCL:Obj_Decl */
/* TAG END SPCL:Obj_Decl */

/*
	Handler for window GEOLOGY_OPTIONS_WINDOW ("Geology Options")
*/
long XVT_CALLCONV1
#if XVT_CC_PROTO
GEOLOGY_OPTIONS_WINDOW_eh XVT_CALLCONV2 (WINDOW xdWindow, EVENT *xdEvent)
#else
GEOLOGY_OPTIONS_WINDOW_eh XVT_CALLCONV2 (xdWindow, xdEvent)
WINDOW xdWindow;
EVENT *xdEvent;
#endif
{
	short xdControlId = xdEvent->v.ctl.id;
	/* TAG BEGIN SPCL:Var_Decl */
	/* TAG END SPCL:Var_Decl */

	switch (xdEvent->type) {
	case E_CREATE:
		/*
			Window has been created; first event sent to newly-created
			window.
		*/
		{
		/* TAG BEGIN EVNT:Create */
		      int mmin, mmax;
		         /* put this window in the created heirachy */
		      addWinToStack (xdWindow);
		         /* ******************************** */
		         /* set the ranges of the scrollbars */
		                     /* **************** */
		      mmin = -10000; mmax = 10000;     /* range for Chair  */
		      setScrollRange (xdWindow, WIN_116_HSCROLL_51, mmin, mmax);
		      setScrollRange (xdWindow, WIN_116_HSCROLL_52, mmin, mmax);
		      setScrollRange (xdWindow, WIN_116_HSCROLL_53, mmin, mmax);
		                     /* ******************** */
		      mmin = 0; mmax = 180;         /* range for Well Angle */
		      setScrollRange (xdWindow, WIN_116_HSCROLL_73, mmin, mmax);
		                     /* ********************* */
		      mmin = 0; mmax = 360;         /* range for Well Declin */
		      setScrollRange (xdWindow, WIN_116_HSCROLL_74, mmin, mmax);
		                     /* ******************** */
		      mmin = 0; mmax = 20000;       /* range for Well depth */
		      setScrollRange (xdWindow, WIN_116_HSCROLL_75, mmin, mmax);
		               /* *********************************** */
		               /* setup all the options in the window */
		      loadGeologyOptions (xdWindow);
		            /* *************************************************** */
		            /* Check the consistency of scrollbars and text fields */
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_51, WIN_116_EDIT_57, FALSE);
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_52, WIN_116_EDIT_59, FALSE);
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_53, WIN_116_EDIT_58, FALSE);
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_73, WIN_116_EDIT_76, FALSE);
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_74, WIN_116_EDIT_78, FALSE);
		      updateScrollAndText (xdWindow, WIN_116_HSCROLL_75, WIN_116_EDIT_77, FALSE);
		/* TAG END EVNT:Create */
		}
		break;
	case E_DESTROY:
		/*
			Window has been closed; last event sent to window.
		*/
		xdRemoveHelpAssoc( xdWindow );
		{
		/* TAG BEGIN EVNT:Destroy */
		         /* take this window out of the created heirachy */
		      takeWinFromStack (xdWindow);
		/* TAG END EVNT:Destroy */
		}
		break;
	case E_FOCUS:
		{
		/* TAG BEGIN EVNT:Focus */
		      /*
		         Window has lost or gained focus.
		      */
		      if (xdEvent->v.active) {
		         /*
		            Window has gained focus
		         */
		      } else {
		         /*
		            Window has lost focus
		         */
		      }
		/* TAG END EVNT:Focus */
		}
		break;
	case E_SIZE:
		/*
			Size of window has been set or changed; sent when window is
			created or subsequently resized by user or via xvt_vobj_move.
		*/
		{
		/* TAG BEGIN EVNT:Size */
		/* TAG END EVNT:Size */
		}
		break;
	case E_UPDATE:
		/*
			Window requires updating.
		*/
		{
		/* TAG BEGIN EVNT:Update */
		      xvt_dwin_clear(xdWindow, (COLOR)xvt_vobj_get_attr(xdWindow, ATTR_BACK_COLOR));
		/* TAG END EVNT:Update */
		}
		break;
	case E_CLOSE:
		/*
			Request to close window; user operated "close" menu item on
			window system menu, or operated "close" control on window
			frame. Not sent if Close on File menu is issued. Window not
			closed unless xvt_vobj_destroy is called.
		*/
		{
		/* TAG BEGIN EVNT:Close */
		      xvt_vobj_destroy(xdWindow);
		/* TAG END EVNT:Close */
		}
		break;
	case E_CHAR:
		/*
			Character typed.
		*/
		{
		/* TAG BEGIN EVNT:Char */
		      
		         /* *********************************************** */
		         /* make a RETURN key activate the window OK Button */
		      if (xdEvent->v.chr.ch == '\r')
		      {
		         xdEvent->type = E_CONTROL;
		         xdEvent->v.ctl.id = WIN_116_PUSHBUTTON_37;
		         xvt_win_dispatch_event (xdWindow, xdEvent);
		      }
		/* TAG END EVNT:Char */
		}
		break;
	case E_MOUSE_UP:
		/*
			Mouse was released
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Up */
		/* TAG END EVNT:Mouse_Up */
		}
		break;
	case E_MOUSE_DOWN:
		/*
			Mouse was pressed
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Down */
		/* TAG END EVNT:Mouse_Down */
		}
		break;
	case E_MOUSE_DBL:
		/*
			Mouse was double clicked
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Dbl */
		/* TAG END EVNT:Mouse_Dbl */
		}
		break;
	case E_MOUSE_MOVE:
		/*
			Mouse was moved
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Move */
		/* TAG END EVNT:Mouse_Move */
		}
		break;
	case E_HSCROLL:
		{
		/* TAG BEGIN EVNT:Hscroll */
		      /*
		         Horizontal scrollbar on frame was operated
		      */
		      switch (xdEvent->v.scroll.what) {
		      case SC_LINE_UP:
		         break;
		      case SC_LINE_DOWN:
		         break;
		      case SC_PAGE_UP:
		         break;
		      case SC_PAGE_DOWN:
		         break;
		      case SC_THUMB:
		         break;
		      case SC_THUMBTRACK:
		         break;
		      default:
		         break;
		      }
		/* TAG END EVNT:Hscroll */
		}
		break;
	case E_VSCROLL:
		{
		/* TAG BEGIN EVNT:Vscroll */
		      /*
		         Vertical scrollbar on frame was operated
		      */
		      switch (xdEvent->v.scroll.what) {
		      case SC_LINE_UP:
		         break;
		      case SC_LINE_DOWN:
		         break;
		      case SC_PAGE_UP:
		         break;
		      case SC_PAGE_DOWN:
		         break;
		      case SC_THUMB:
		         break;
		      case SC_THUMBTRACK:
		         break;
		      default:
		         break;
		      }
		/* TAG END EVNT:Vscroll */
		}
		break;
	case E_COMMAND:
		/*
			User issued command on window menu bar (menu bar at top of
			screen for Mac/CH).
		*/
		{
		/* TAG BEGIN EVNT:Command */
		      /*
		         No menubar was associated with this window
		      */
		/* TAG END EVNT:Command */
		}
		break;
	case E_CONTROL:
		/*
			User operated control in window.
		*/
		{
		/* TAG BEGIN SPCL:Control_Decl */
		/* TAG END SPCL:Control_Decl */

		switch(xdControlId) {
		case WIN_116_PUSHBUTTON_37: /* "OK" */
			{
			/* TAG BEGIN WIN_116_PUSHBUTTON_37 EVNT:Control */
			                  /* ********************************** */
			                  /* save all the options in the window */
			         saveGeologyOptions (xdWindow);
			         xvt_vobj_destroy(xdWindow);
			/* TAG END WIN_116_PUSHBUTTON_37 EVNT:Control */
			}
			break;
		case WIN_116_PUSHBUTTON_38: /* "Cancel" */
			{
			/* TAG BEGIN WIN_116_PUSHBUTTON_38 EVNT:Control */
			         xvt_vobj_destroy(xdWindow);
			/* TAG END WIN_116_PUSHBUTTON_38 EVNT:Control */
			}
			break;
		case WIN_116_PUSHBUTTON_39: /* "Help..." */
			{
			/* TAG BEGIN WIN_116_PUSHBUTTON_39 EVNT:Control */
			displayHelp("geoldisp.htm");
			/* TAG END WIN_116_PUSHBUTTON_39 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_51: /* "Horizontal Scrollbar CX" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_51 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - THOUSAND;
			            SNAP_DOWN_BY(pos, 1000)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + THOUSAND;
			            SNAP_UP_BY(pos, 1000)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_57, pos);
			/* TAG END WIN_116_HSCROLL_51 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_52: /* "Horizontal Scrollbar CY" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_52 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - THOUSAND;
			            SNAP_DOWN_BY(pos, 1000)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + THOUSAND;
			            SNAP_UP_BY(pos, 1000)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_59, pos);
			/* TAG END WIN_116_HSCROLL_52 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_53: /* "Horizontal Scrollbar CZ" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_53 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - THOUSAND;
			            SNAP_DOWN_BY(pos, 1000)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + THOUSAND;
			            SNAP_UP_BY(pos, 1000)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_58, pos);
			/* TAG END WIN_116_HSCROLL_53 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_57: /* "0" */
			{
			/* TAG BEGIN WIN_116_EDIT_57 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_51, WIN_116_EDIT_57, FALSE);
			         }
			/* TAG END WIN_116_EDIT_57 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_58: /* "0" */
			{
			/* TAG BEGIN WIN_116_EDIT_58 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_53, WIN_116_EDIT_58, FALSE);
			         }
			/* TAG END WIN_116_EDIT_58 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_59: /* "0" */
			{
			/* TAG BEGIN WIN_116_EDIT_59 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_52, WIN_116_EDIT_59, FALSE);
			         }
			/* TAG END WIN_116_EDIT_59 EVNT:Control */
			}
			break;
		case WIN_116_CHECKBOX_68: /* "Use Topography" */
			{
			/* TAG BEGIN WIN_116_CHECKBOX_68 EVNT:Control */
			         int gotTopo = FALSE;
			         OBJECT *data = (OBJECT *) xvt_vobj_get_data (xdWindow);
			         WINDOW win;
			         WINDOW ctl_win = xvt_win_get_ctl(xdWindow, WIN_116_CHECKBOX_68);
			         int checked;
			         checked = !xvt_ctl_is_checked (ctl_win);
			         xvt_ctl_set_checked(ctl_win, (BOOLEAN) checked);
			         if (checked)
			         {
			            static FILE_SPEC fs;
#if (XVTWS == MACWS)
			            strcpy (fs.type, "TEXT");
#else
			            strcpy (fs.type, "top");
#endif
			            getDefaultDirectory(&(fs.dir));
			            switch (xvt_dm_post_file_open(&fs, "Read Topography from...")) {
			            case FL_OK:
			               xvt_fsys_set_dir(&fs.dir);
			               setDefaultDirectory(&(fs.dir));
			               gotTopo = loadTopographyFile(&fs);
			               break;
			            case FL_BAD:
			               xvt_dm_post_error("Error getting file name.");
			               break;
			            case FL_CANCEL:
			               break;
			            }
			         }
			         if (gotTopo)
			         {
			            if (!(win = createCenteredWindow(TOPO_OPTIONS_WINDOW, TASK_WIN, EM_ALL, TOPO_OPTIONS_WINDOW_eh, (long) data)))
			            xvt_dm_post_error("Can't open window");
			         }
			         else
			            xvt_ctl_set_checked(ctl_win, FALSE);
			/* TAG END WIN_116_CHECKBOX_68 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_73: /* "Horizontal Scrollbar W Angle" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_73 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos =xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - TEN;
			            SNAP_DOWN_BY(pos, 10)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + TEN;
			            SNAP_UP_BY(pos, 10)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_76, pos);
			/* TAG END WIN_116_HSCROLL_73 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_74: /* "Horizontal Scrollbar Declination" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_74 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - TEN;
			            SNAP_DOWN_BY(pos, 10)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + TEN;
			            SNAP_UP_BY(pos, 10)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_78, pos);
			/* TAG END WIN_116_HSCROLL_74 EVNT:Control */
			}
			break;
		case WIN_116_HSCROLL_75: /* "Horizontal Scrollbar Depth" */
			{
			/* TAG BEGIN WIN_116_HSCROLL_75 EVNT:Control */
			         int pos = 0;
			         /*
			            Horizontal scrollbar control was operated
			         */
			         switch (xdEvent->v.ctl.ci.v.scroll.what) {
			         case SC_LINE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
			            break;
			         case SC_LINE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
			            break;
			         case SC_PAGE_UP:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - THOUSAND;
			            SNAP_DOWN_BY(pos, 1000)
			            break;
			         case SC_PAGE_DOWN:
			            pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + THOUSAND;
			            SNAP_UP_BY(pos, 1000)
			            break;
			         case SC_THUMB:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         case SC_THUMBTRACK:
			            pos = xdEvent->v.ctl.ci.v.scroll.pos;
			            break;
			         }
			         updateScrollField (xdWindow, xdControlId, pos);
			         updateNumericTextField (xdWindow, WIN_116_EDIT_77, pos);
			/* TAG END WIN_116_HSCROLL_75 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_76: /* "0.0" */
			{
			/* TAG BEGIN WIN_116_EDIT_76 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_73, WIN_116_EDIT_76, TRUE);
			         }
			/* TAG END WIN_116_EDIT_76 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_77: /* "5000" */
			{
			/* TAG BEGIN WIN_116_EDIT_77 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_75, WIN_116_EDIT_77, FALSE);
			         }
			/* TAG END WIN_116_EDIT_77 EVNT:Control */
			}
			break;
		case WIN_116_EDIT_78: /* "90" */
			{
			/* TAG BEGIN WIN_116_EDIT_78 EVNT:Control */
			         /*
			            Edit control was operated.
			         */
			         if (xdEvent->v.ctl.ci.v.edit.focus_change) {
			            if (xdEvent->v.ctl.ci.v.edit.active) {
			               /*
			                  focus has entered the control
			               */
			            } else {
			               /*
			                  focus has left the control
			               */
			            }
			         } else {
			            /*
			               Contents of control were changed
			            */
			         updateScrollAndText (xdWindow, WIN_116_HSCROLL_74, WIN_116_EDIT_78, TRUE);
			         }
			/* TAG END WIN_116_EDIT_78 EVNT:Control */
			}
			break;
		case GEOLOGY_OPTIONS_WINDOW_C1: /* " Use Chair Diagram" */
			{
			/* TAG BEGIN GEOLOGY_OPTIONS_WINDOW_C1 EVNT:Control */
			         WINDOW ctl_win = xvt_win_get_ctl(xdWindow, GEOLOGY_OPTIONS_WINDOW_C1);
			         int state = (!xvt_ctl_is_checked(ctl_win));
			         xvt_ctl_set_checked(ctl_win, (BOOLEAN) state);
			                  /* enable the options that should be alloud */
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_GROUPBOX_50),(BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_TEXT_54), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_HSCROLL_51), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_EDIT_57), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_TEXT_55), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_HSCROLL_52), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_EDIT_59), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_TEXT_56), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_HSCROLL_53), (BOOLEAN) state);
			         xvt_vobj_set_enabled (xvt_win_get_ctl (xdWindow, WIN_116_EDIT_58), (BOOLEAN) state);
			/* TAG END GEOLOGY_OPTIONS_WINDOW_C1 EVNT:Control */
			}
			break;
		case GEOLOGY_BLOCK_VIEW: /* "Block View Options..." */
			{
			/* TAG BEGIN GEOLOGY_BLOCK_VIEW EVNT:Control */
			         WINDOW win;
			         if (!(win = createCenteredWindow(VIEW_WINDOW, TASK_WIN, EM_ALL, VIEW_WINDOW_eh, 0L)))
			            xvt_dm_post_error("Can't open window");
			/* TAG END GEOLOGY_BLOCK_VIEW EVNT:Control */
			}
			break;
		default:
			break;
		}
		}
		break;
	case E_FONT:
		/*
			User issued font command on window menu bar (menu bar at top of
			screen for Mac/CH).
		*/
		{
		/* TAG BEGIN EVNT:Font */
		/* TAG END EVNT:Font */
		}
		break;
	case E_TIMER:
		/*
			Timer associated with window went off.
		*/
		{
		/* TAG BEGIN EVNT:Timer */
		/* TAG END EVNT:Timer */
		}
		break;
	case E_USER:
		/*
			Application initiated.
		*/
		{
		/* TAG BEGIN EVNT:User */
		      switch (xdEvent->v.user.id) {
		      case -1:
		      default:
		         break;
		      }
		/* TAG END EVNT:User */
		}
		break;
	default:
		break;
	}
	/* TAG BEGIN SPCL:Bottom */
#ifdef XVT_R40_TXEDIT_BEHAVIOR
	   xvt_tx_process_event(xdWindow, xdEvent);
#endif
	/* TAG END SPCL:Bottom */
	return 0L;
}
