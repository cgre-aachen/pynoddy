/*
	This file was generated by XVT-Design 4.5, a product of:

		XVT Software Inc.
		4900 Pearl East Circle
		Boulder, CO USA 80301
		303-443-4223, fax 303-443-0969

	Generated on Tue Sep 02 10:34:59 1997
*/
/* TAG BEGIN SPCL:Pre_Header */
/* TAG END SPCL:Pre_Header */

#include "xvt.h"
#include "xvtcm.h"
#include "nodInc.h"

/* 
	Information about the window
*/
#define WIN_RES_ID DEFINE_COLOUR_WINDOW
#define WIN_FLAGS 0x810L
#define WIN_CLASS ""
#define WIN_BORDER W_DOC
/* TAG BEGIN SPCL:Obj_Decl */
/* TAG END SPCL:Obj_Decl */

/*
	Handler for window DEFINE_COLOUR_WINDOW ("Define Colour")
*/
long XVT_CALLCONV1
#if XVT_CC_PROTO
DEFINE_COLOUR_WINDOW_eh XVT_CALLCONV2 (WINDOW xdWindow, EVENT *xdEvent)
#else
DEFINE_COLOUR_WINDOW_eh XVT_CALLCONV2 (xdWindow, xdEvent)
WINDOW xdWindow;
EVENT *xdEvent;
#endif
{
	short xdControlId = xdEvent->v.ctl.id;
	/* TAG BEGIN SPCL:Var_Decl */
	/* TAG END SPCL:Var_Decl */

	switch (xdEvent->type) {
	case E_CREATE:
		/*
			Window has been created; first event sent to newly-created
			window.
		*/
		{
		/* TAG BEGIN EVNT:Create */
		WINDOW ctlwin;
		int mmin, mmax;
			/* put this window in the created heirachy */
		addWinToStack (xdWindow);
			/* put the options into the list selection */
		makeColorList (xdWindow, DEFINE_COLOUR_WINDOW_LISTEDIT_41);
		makeColorSamples (xdWindow);
			/* ******************************** */
			/* set the ranges of the scrollbars */
						/* ******************** */
		mmin = 0; mmax = 255;		/* range for RGB Values */
		setScrollRange (xdWindow, WIN_104_HSCROLL_5, mmin, mmax);
		setScrollRange (xdWindow, WIN_104_HSCROLL_6, mmin, mmax);
		setScrollRange (xdWindow, WIN_104_HSCROLL_7, mmin, mmax);
					/* *********************************** */
					/* setup all the options in the window */
		loadWindowOptions (xdWindow, DEFINE_COLOUR_WINDOW, (OBJECT *) xvt_vobj_get_data (xdWindow));
		/* TAG END EVNT:Create */
		}
		break;
	case E_DESTROY:
		/*
			Window has been closed; last event sent to window.
		*/
		xdRemoveHelpAssoc( xdWindow );
		{
		/* TAG BEGIN EVNT:Destroy */
			/* take this window out of the created heirachy */
		destroyColorSamples (xdWindow);
		takeWinFromStack (xdWindow);
		/* TAG END EVNT:Destroy */
		}
		break;
	case E_FOCUS:
		{
		/* TAG BEGIN EVNT:Focus */
		/*
			Window has lost or gained focus.
		*/
		if (xdEvent->v.active) {
			/*
				Window has gained focus
			*/
		} else {
			/*
				Window has lost focus
			*/
		}
		/* TAG END EVNT:Focus */
		}
		break;
	case E_SIZE:
		/*
			Size of window has been set or changed; sent when window is
			created or subsequently resized by user or via xvt_vobj_move.
		*/
		{
		/* TAG BEGIN EVNT:Size */
		/* TAG END EVNT:Size */
		}
		break;
	case E_UPDATE:
		/*
			Window requires updating.
		*/
		{
		/* TAG BEGIN EVNT:Update */
		extern void drawSelectedColorGrid();
#if (XVTWS == WINWS) || (XVTWS == WIN16WS) || (XVTWS == WIN32WS)
		xvt_dwin_clear(xdWindow, (COLOR)xvt_vobj_get_attr(xdWindow, ATTR_BACK_COLOR));
#endif
		drawSelectedColorGrid (xdWindow);
		drawDefinedColorGrid (xdWindow);
		drawCustomColorGrid (xdWindow);
		/* TAG END EVNT:Update */
		}
		break;
	case E_CLOSE:
		/*
			Request to close window; user operated "close" menu item on
			window system menu, or operated "close" control on window
			frame. Not sent if Close on File menu is issued. Window not
			closed unless xvt_vobj_destroy is called.
		*/
		{
		/* TAG BEGIN EVNT:Close */
		xvt_vobj_destroy(xdWindow);
		/* TAG END EVNT:Close */
		}
		break;
	case E_CHAR:
		/*
			Character typed.
		*/
		{
		/* TAG BEGIN EVNT:Char */
			/* *********************************************** */
			/* make a RETURN key activate the window OK Button */
		if (xdEvent->v.chr.ch == '\r')
		{
		   xdEvent->type = E_CONTROL;
		   xdEvent->v.ctl.id = DEFINE_COLOUR_WINDOW_P_C12;
		   xvt_win_dispatch_event (xdWindow, xdEvent);
		}
		/* TAG END EVNT:Char */
		}
		break;
	case E_MOUSE_UP:
		/*
			Mouse was released
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Up */
			/* ****************************************** */
			/* if they released on a color then select it */
		interactivelySelectColor (xdWindow, xdEvent->v.mouse.where);
		/* TAG END EVNT:Mouse_Up */
		}
		break;
	case E_MOUSE_DOWN:
		/*
			Mouse was pressed
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Down */
		/* TAG END EVNT:Mouse_Down */
		}
		break;
	case E_MOUSE_DBL:
		/*
			Mouse was double clicked
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Dbl */
		/* TAG END EVNT:Mouse_Dbl */
		}
		break;
	case E_MOUSE_MOVE:
		/*
			Mouse was moved
		*/
		{
		/* TAG BEGIN EVNT:Mouse_Move */
		/* TAG END EVNT:Mouse_Move */
		}
		break;
	case E_HSCROLL:
		{
		/* TAG BEGIN EVNT:Hscroll */
		/*
			Horizontal scrollbar on frame was operated
		*/
		switch (xdEvent->v.scroll.what) {
		case SC_LINE_UP:
			break;
		case SC_LINE_DOWN:
			break;
		case SC_PAGE_UP:
			break;
		case SC_PAGE_DOWN:
			break;
		case SC_THUMB:
			break;
		case SC_THUMBTRACK:
			break;
		default:
			break;
		}
		/* TAG END EVNT:Hscroll */
		}
		break;
	case E_VSCROLL:
		{
		/* TAG BEGIN EVNT:Vscroll */
		/*
			Vertical scrollbar on frame was operated
		*/
		switch (xdEvent->v.scroll.what) {
		case SC_LINE_UP:
			break;
		case SC_LINE_DOWN:
			break;
		case SC_PAGE_UP:
			break;
		case SC_PAGE_DOWN:
			break;
		case SC_THUMB:
			break;
		case SC_THUMBTRACK:
			break;
		default:
			break;
		}
		/* TAG END EVNT:Vscroll */
		}
		break;
	case E_COMMAND:
		/*
			User issued command on window menu bar (menu bar at top of
			screen for Mac/CH).
		*/
		{
		/* TAG BEGIN EVNT:Command */
		/*
			No menubar was associated with this window
		*/
		/* TAG END EVNT:Command */
		}
		break;
	case E_CONTROL:
		/*
			User operated control in window.
		*/
		{
		/* TAG BEGIN SPCL:Control_Decl */
		/* TAG END SPCL:Control_Decl */

		switch(xdControlId) {
		case WIN_104_HSCROLL_5: /* "Horizontal Scrollbar Red" */
			{
			/* TAG BEGIN WIN_104_HSCROLL_5 EVNT:Control */
			int pos = 0;
			NODDY_COLOUR color;
			/*
				Horizontal scrollbar control was operated
			*/
			switch (xdEvent->v.ctl.ci.v.scroll.what) {
			case SC_LINE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
				break;
			case SC_LINE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
				break;
			case SC_PAGE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 10;
				break;
			case SC_PAGE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 10;
				break;
			case SC_THUMB:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			case SC_THUMBTRACK:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			}
			updateScrollField (xdWindow, xdControlId, pos);
			getColorSelection (xdWindow, &color);
			setCustomColor (xdWindow, color);
			/* TAG END WIN_104_HSCROLL_5 EVNT:Control */
			}
			break;
		case WIN_104_HSCROLL_6: /* "Horizontal Scrollbar Green" */
			{
			/* TAG BEGIN WIN_104_HSCROLL_6 EVNT:Control */
			int pos = 0;
			NODDY_COLOUR color;
			/*
				Horizontal scrollbar control was operated
			*/
			switch (xdEvent->v.ctl.ci.v.scroll.what) {
			case SC_LINE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
				break;
			case SC_LINE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
				break;
			case SC_PAGE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 10;
				break;
			case SC_PAGE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 10;
				break;
			case SC_THUMB:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			case SC_THUMBTRACK:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			}
			updateScrollField (xdWindow, xdControlId, pos);
			getColorSelection (xdWindow, &color);
			setCustomColor (xdWindow, color);
			/* TAG END WIN_104_HSCROLL_6 EVNT:Control */
			}
			break;
		case WIN_104_HSCROLL_7: /* "Horizontal Scrollbar Blue" */
			{
			/* TAG BEGIN WIN_104_HSCROLL_7 EVNT:Control */
			int pos = 0;
			NODDY_COLOUR color;
			/*
				Horizontal scrollbar control was operated
			*/
			switch (xdEvent->v.ctl.ci.v.scroll.what) {
			case SC_LINE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 1;
				break;
			case SC_LINE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 1;
				break;
			case SC_PAGE_UP:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) - 10;
				break;
			case SC_PAGE_DOWN:
				pos = xvt_sbar_get_pos (xdEvent->v.ctl.ci.win, HVSCROLL) + 10;
				break;
			case SC_THUMB:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			case SC_THUMBTRACK:
				pos = xdEvent->v.ctl.ci.v.scroll.pos;
				break;
			}
			updateScrollField (xdWindow, xdControlId, pos);
			getColorSelection (xdWindow, &color);
			setCustomColor (xdWindow, color);
			/* TAG END WIN_104_HSCROLL_7 EVNT:Control */
			}
			break;
		case DEFINE_COLOUR_WINDOW_P_C12: /* "OK" */
			{
			/* TAG BEGIN DEFINE_COLOUR_WINDOW_P_C12 EVNT:Control */
						/* ********************************** */
						/* save all the options in the window */
			saveWindowOptions (xdWindow, DEFINE_COLOUR_WINDOW, (OBJECT *) xvt_vobj_get_data (xdWindow));
			xvt_vobj_destroy(xdWindow);
			/* TAG END DEFINE_COLOUR_WINDOW_P_C12 EVNT:Control */
			}
			break;
		case DEFINE_COLOUR_WINDOW_P_C13: /* "Cancel" */
			{
			/* TAG BEGIN DEFINE_COLOUR_WINDOW_P_C13 EVNT:Control */
			xvt_vobj_destroy(xdWindow);
			/* TAG END DEFINE_COLOUR_WINDOW_P_C13 EVNT:Control */
			}
			break;
		case DEFINE_COLOUR_WINDOW_P_C14: /* "Help..." */
			{
			/* TAG BEGIN DEFINE_COLOUR_WINDOW_P_C14 EVNT:Control */
			displayHelp("colour.htm");
			/* TAG END DEFINE_COLOUR_WINDOW_P_C14 EVNT:Control */
			}
			break;
		case DEFINE_COLOUR_WINDOW_LISTEDIT_41: /* "Colour List" */
			{
			/* TAG BEGIN DEFINE_COLOUR_WINDOW_LISTEDIT_41 EVNT:Control */
			/*
				Listedit control was operated.
			*/
			if (xdEvent->v.ctl.ci.v.edit.focus_change) {
				if (xdEvent->v.ctl.ci.v.edit.active) {
					/*
						focus has entered the control
					*/
				} else {
					/*
						focus has left the control
					*/
				}
			} else {
				/*
					Contents of control were changed
				*/
				NODDY_COLOUR color;
			
				xvt_vobj_get_title (xdEvent->v.ctl.ci.win, color.name, COLOUR_NAME_LENGTH);
				if (!getColorRGB (color.name,
			                        &(color.red), &(color.green), &(color.blue)))
				{
				   color.red = xvt_sbar_get_pos (
			                xvt_win_get_ctl (xdWindow, WIN_104_HSCROLL_5), HVSCROLL);
				   color.green = xvt_sbar_get_pos (
			                xvt_win_get_ctl (xdWindow, WIN_104_HSCROLL_6), HVSCROLL);
				   color.blue = xvt_sbar_get_pos (
			                xvt_win_get_ctl (xdWindow, WIN_104_HSCROLL_7), HVSCROLL);
				}
			
				selectColor (xdWindow, DEFINE_COLOUR_WINDOW_LISTEDIT_41, color);
			}
			/* TAG END DEFINE_COLOUR_WINDOW_LISTEDIT_41 EVNT:Control */
			}
			break;
		default:
			break;
		}
		}
		break;
	case E_FONT:
		/*
			User issued font command on window menu bar (menu bar at top of
			screen for Mac/CH).
		*/
		{
		/* TAG BEGIN EVNT:Font */
		/* TAG END EVNT:Font */
		}
		break;
	case E_TIMER:
		/*
			Timer associated with window went off.
		*/
		{
		/* TAG BEGIN EVNT:Timer */
		/* TAG END EVNT:Timer */
		}
		break;
	case E_USER:
		/*
			Application initiated.
		*/
		{
		/* TAG BEGIN EVNT:User */
		switch (xdEvent->v.user.id) {
		case -1:
		default:
			break;
		}
		/* TAG END EVNT:User */
		}
		break;
	default:
		break;
	}
	/* TAG BEGIN SPCL:Bottom */
#ifdef XVT_R40_TXEDIT_BEHAVIOR
	xvt_tx_process_event(xdWindow, xdEvent);
#endif
	/* TAG END SPCL:Bottom */
	return 0L;
}
